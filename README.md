# liyoclaw1242.github.io

⚠️ **此 repo 為自動部署目標,請勿直接編輯。**

Source 與 build 流程位於 private repo:
**https://github.com/liyoclaw1242/my-site-source**

每次 source repo `main` push 都會把這裡的所有檔案**完全取代**。如果直接在這裡 commit,下次 deploy 就會被覆蓋掉。

要保留某個檔案在這裡(例如 workflow 或 meta 檔),請加到 source repo 的 `static-extras/` 目錄內。
